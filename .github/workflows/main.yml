name: Pipeline de Execução

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Run executable
      run: |
        chmod +x C:\Users\Usuario\source\repos\DbUpVersionadorSQL\DbUpVersionadorSQL\bin\Release\DbUpVersionadorSQL.exe
        C:\Users\Usuario\source\repos\DbUpVersionadorSQL\DbUpVersionadorSQL\bin\Release\DbUpVersionadorSQL.exe